import React, { Component, StrictMode } from "react";
import Web from "./Web";

export default class App extends Component {
  render() {
    return (
      <StrictMode>
        <Web />
      </StrictMode>
    );
  }
}